#wrapper
  h2 remotes
  #remotes
  h2 apps
  #apps
  h2 procs
  #procs
  #debug

template(id="nexus"):
  div #{JSON.stringify(locals)}

template(id="remote"):
  li.remote #{JSON.stringify(locals)}
  
template(id="app"):
  li.app #{id} - #{name}
   
template(id="proc"):
  li.proc #{id} - #{name} - #{script} - #{uptime}


